sudo: false
language: cpp
os: osx
compiler:
  - clang
  - gcc
branches:
  only:
    - master
before_install:
  - cd qsl && sh build/bootstrap-osx-homebrew.sh
script:
  - cd qsl && sh build/rebuild_and_check.sh
notifications:
  email:
    on_success: never
    on_failure: always
